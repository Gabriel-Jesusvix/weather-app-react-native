name: CD

on:
  push:
    branches:
      - develop

jobs:
  CD: 
    name: Continuous Delivery
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3